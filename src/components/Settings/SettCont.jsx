import React from "react"
import {connect} from "react-redux"

let SettCont=props=>{
    return <div></div>
}

let MapState=state=>{
    return{
        
    }
}

export default connect(MapState,{})(SettCont)